[project]
name = "mushaf-konnekt-backend"
version = "0.1.0"
description = "Connecting to Mushaf, one verse at a time :)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
    "thefuzz>=0.22.1",
]
